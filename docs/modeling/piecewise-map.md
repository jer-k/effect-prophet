# Linear piecewise MAP model

This document fixes the Stage C numerical contract that the Rust/WASM implementation will
consume. It is a specification and fixture reference, not a TypeScript numerical
implementation. TypeScript will parse public inputs, invoke coarse WASM operations, decode their
frames, and construct trusted model state; trend evaluation and fitting belong to Rust.

## Fixed-parameter piecewise trend

For training timestamps with first timestamp `origin` and last timestamp `end`, define

```text
scale = end - origin
u(timestamp) = (timestamp - origin) / scale
c_j = u(changepoint_j)
```

`scale` must be positive. Explicit changepoints are canonical UTC timestamps represented at the
numerical boundary as integer epoch milliseconds. They are strictly increasing, unique, and
inside the inclusive training interval `[origin, end]`. They do not need to coincide with an
observation.

For output-unit intercept `m`, slope `k`, and aligned output-unit rate adjustments `delta_j`, the
continuous trend is

```text
trend(u) = m + k*u + sum_j(delta_j * max(0, u-c_j))
```

The hinge is zero at its changepoint and active to its right. Consequently, the trend is
continuous and its one-sided slope changes by `delta_j`. The slope and deltas express change per
one complete training-time interval, not change per millisecond.

A changepoint at `origin` makes the base slope and first adjustment redundant in the training
design. A changepoint at `end` has a zero training column but affects extrapolation to the right.
Both endpoints are permitted because Prophet 1.4.0 accepts explicit changepoints on its inclusive
training bounds. Identifiability and prior behavior belong to the MAP fitting contract; the
fixed evaluator does not rewrite endpoint parameters.

An empty changepoint list is a genuine zero-column evaluation state:

```text
trend(u) = m + k*u
```

Prophet's private dummy zero-time changepoint used during fitting is not a public candidate. The
MAP optimizer decision must separately resolve whether fitting uses a private dummy and parameter
folding or a true-empty reduced objective. Fixed evaluation does not depend on that decision.

Prediction timestamps may be historical or future and may be unsorted or repeated at the public
API. Evaluation reuses the stored training origin and scale, preserves request order, and does not
clip extrapolation. The committed Prophet fixtures are ordered because Prophet's dataframe
preprocessing sorts rows; Rust property and boundary tests own the package-specific order and
duplicate guarantees.

## Additive Fourier composition

Fourier features retain the existing epoch-relative day coordinate and ordered
sine-before-cosine columns. They do not use the trend's training-relative coordinate. Given
ordered component matrix `X` and output-unit coefficients `beta`,

```text
component_s = X_s * beta_s
additive = sum_s(component_s)
value = trend + additive
```

Changepoint effects are part of `trend`; they are not an additive seasonal component.

## Fixed Prophet evidence

[`piecewise-linear.json`](../../integration/fixtures/prophet-1.4.0/piecewise-linear.json) is
generated by the pinned Prophet 1.4.0 environment. Each case records:

- training observations used only to establish Prophet's time and absmax target scales;
- explicit changepoint timestamps and their scaled coordinates;
- output-unit intercept, slope, deltas, and optional seasonal coefficients;
- Prophet-scaled training and prediction coordinates;
- Prophet's fixed-parameter trend;
- Fourier columns, named component values, additive totals, and reconstructed forecasts.

The generator converts output-unit trend parameters to Prophet's scaled parameterization and then
calls the release's `setup_dataframe` and `predict_trend`. Fourier columns come from the release's
`fourier_series`. No optimizer or model fit is invoked, so this evidence establishes fixed
evaluation parity only.

The cases cover two interior breaks on irregular history with multiple seasonal components,
inclusive start/end changepoints, historical and future extrapolation, and reduction to a linear
trend with no changepoints. Initial comparison tolerance is

```text
abs(actual - expected) <= 1e-10 + 1e-10*abs(expected)
```

Rust implementation work must also independently test hand-calculated lines, continuity from both
sides, one-sided slopes, malformed alignment, checked-size failures, non-finite arithmetic, empty
prediction batches, unsorted requests, repeated requests, and input ownership. Fixture agreement
alone is not sufficient evidence of a correct implementation.

## Ownership boundaries

- **Reference tooling:** Prophet source identity, fixed parameters, expected values, and fixture
  provenance.
- **Rust kernel:** changepoint validation after public syntax parsing, scaling, hinge evaluation,
  MAP objective and optimization, component evaluation, and numerical failures.
- **WASM binding:** checked scalar/array conversion and exact packed success/failure frames.
- **TypeScript adapter:** canonical timestamp/options parsing, coarse invocation, strict frame
  decoding, trusted immutable model construction, persistence, Effect errors, and boundary spans.

No TypeScript trend evaluator, objective evaluator, or optimizer is part of this design.

## MAP fitting

The accepted [`piecewise-map-coordinate-v1` decision](../decisions/map-optimizer.md) extends the
fixed trend with Prophet's normal trend/feature priors, exact Laplace changepoint prior, positive
observation noise, and normal likelihood. Rust uses deterministic proximal coordinate updates and
the exact conditional noise update; there is no fallback to another fitting objective or smoothed absolute value.

The implementation uses a true empty changepoint design rather than Prophet's private dummy-delta
fit parameterization. No-point fixed evaluation remains identical, but no-point fitted objective
or parameter parity is not claimed. Exact constant histories use an explicit
`constant-target-shortcut`; nonconstant residual collapse and iteration exhaustion are typed
failures rather than successful models.

## Rust/WASM protocol

The coarse fit export accepts training timestamps/values, explicit or automatic changepoint
controls, ordered seasonality metadata, the changepoint prior, and deterministic optimizer
controls. Rust resolves automatic candidates and returns their timestamps with fitted deltas.

Fit success has exact width `13 + 2*C + K`:

```text
[
  0, C, intercept, slope, timeOrigin, timeScale, valueScale, noiseScale,
  observationCount, iterations, objective, stationarityResidual, termination,
  ...C changepointTimestamps, ...C deltas, ...K seasonalCoefficients
]
```

Termination is zero for convergence and one for the constant-target shortcut. Fit status codes
preserve insufficient history, malformed alignment/observations/configuration, zero time range,
size overflow, non-finite results, noise collapse, and non-convergence.

Prediction accepts complete stored model state and returns exact width `1 + N*(3+S)` with rows
`[trend, additive, value, ...SComponents]`. Indexed timestamp and non-finite failures identify the
request row. Public empty prediction requests return before loading WASM; direct binding calls
still validate supplied model metadata.

Featureless linear requests without `map` retain OLS. When linear additive features are present,
omitting `map` selects automatic changepoints `{ count: 25, range: 0.8 }`; explicit `map` controls
remain available, and `map: {}` means an explicit empty changepoint set. All requests produce the
persisted `linear-piecewise-map` model kind, and decode/prediction never rerun candidate selection.
