[project]
name = "effect-prophet-benchmark"
version = "0.0.0"
description = "Pinned Python Prophet environment for public API comparisons"
requires-python = "==3.12.*"
dependencies = ["prophet==1.4.0"]

[tool.uv]
package = false
