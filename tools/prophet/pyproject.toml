[project]
name = "effect-prophet-reference-fixtures"
version = "0.0.0"
description = "Pinned Python environment for effect-prophet reference fixtures"
requires-python = "==3.12.*"
dependencies = ["prophet==1.4.0"]

[tool.uv]
package = false
